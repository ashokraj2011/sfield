{
  "name": "support-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env --disable-warning=ExperimentalWarning --experimental-strip-types app.ts",
    "validate": "sfield validate",
    "doctor": "sfield doctor"
  },
  "dependencies": {
    "@sfield/core": "^0.1.0",
    "@sfield/preset-local": "^0.1.0",
    "@sfield/cli": "^0.1.0"
  }
}
